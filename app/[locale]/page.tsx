import Link from "next/link";
import { notFound } from "next/navigation";
import { SiteFooter, SiteHeader } from "@/components/site-chrome";
import { copy, isLocale } from "@/lib/i18n";
export default async function Home({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; if (!isLocale(locale)) notFound(); const text = copy[locale].home; return <main><SiteHeader locale={locale} /><section className="hero"><p className="eyebrow">{text.eyebrow}</p><h1>{text.title}<br /><em>{text.titleEmphasis}</em></h1><p className="hero-copy">{text.intro}</p><div className="actions"><Link className="button button-dark" href={`/${locale}/directory/breweries`}>{text.explore} <span>→</span></Link><Link className="button button-quiet" href={`/${locale}/contribute`}>{text.add}</Link></div><div className="hero-note"><span className="pulse" /> {text.note}</div></section><section className="intro grid-section"><div><p className="eyebrow">{text.recordEyebrow}</p><h2>{text.recordTitle}</h2></div><p>{text.recordCopy}</p></section><section className="principles">{text.principles.map(([title, description], index) => <article key={title}><span>0{index + 1}</span><h3>{title}</h3><p>{description}</p></article>)}</section><section className="contribute-banner"><div><p className="eyebrow">{text.bannerEyebrow}</p><h2>{text.bannerTitle}<br />{text.bannerTitleSecond}</h2></div><Link className="round-link" href={`/${locale}/contribute`} aria-label={text.add}>↗</Link></section><SiteFooter locale={locale} /></main>; }
